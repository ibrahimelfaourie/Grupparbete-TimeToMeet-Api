<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iot.timetomeet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" android:label="Login"></activity>
        <activity android:name=".UserRegistrationActivity" android:label="User Registration"/>
        <activity android:name=".BookingConfirmationActivity" android:label="Booking Confirmation"/>
        <activity android:name=".UserReservationActivity" />
        <activity android:name=".RoomDetailsActivity" android:label="Room Details"/>
        <activity
            android:name=".PlantDetailsActivity"
            android:label="@string/plants_available" />
        <activity android:name=".PlantSearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>